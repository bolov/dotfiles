[user]
	name = Mihail Bălan
	email = mihail.balan@gmail.com
[core]
	editor = "if hash gvim >/dev/null 2>&1 ; then EDITOR='gvim -f'; else EDITOR='vim'; fi; $EDITOR"

	quotepath = false
[alias]
	lg = log --all --oneline --decorate --graph
[push]
	default = simple
[diff]
	tool = "if hash gvimdiff >/dev/null 2>&1 ; then TOOL='gvimdiff '; else TOOL='vimdiff'; fi; $TOOL"
[merge]
	tool = "if hash gvimdiff >/dev/null 2>&1 ; then TOOL='gvimdiff '; else TOOL='vimdiff'; fi; $TOOL"
